language: elixir
elixir:
  - 1.6
otp_release:
  - 21.0
branches:
  only:
    - master
install:
  - epmd -daemon
  - mix local.hex --force
  - mix local.rebar --force
script: make travis
env:
  - MIX_ENV=test
	- MIX_ENV=dev
	- MIX_ENV=staging
	- MIX_ENV=prod
before_script: make travis-init
